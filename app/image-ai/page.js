"use client";import {useState} from "react";export default function ImageAI(){const[prompt,setPrompt]=useState("");const[image,setImage]=useState(null);const generate=async()=>{const res=await fetch("/api/image",{method:"POST",body:JSON.stringify({prompt})});const data=await res.json();setImage("data:image/png;base64,"+data.image);};return(<div className="max-w-2xl mx-auto p-6"><h1 className="text-3xl font-bold mb-4">AI Image Generator</h1><textarea value={prompt} onChange={e=>setPrompt(e.target.value)} className="w-full p-3 bg-gray-700 rounded-xl" placeholder="Describe your image..."></textarea><button onClick={generate} className="w-full mt-3 bg-purple-600 hover:bg-purple-700 py-3 rounded-xl font-bold">Generate Image</button>{image&&<img src={image} className="mt-5 rounded-xl shadow-xl"/>}</div>);}